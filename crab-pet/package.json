{
  "name": "feishu-crab-pet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "typecheck": "tsc",
    "build": "npm run typecheck && vite build",
    "test": "npm run typecheck && node --test dist-ts/**/*.test.js",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vite": "^8.0.16"
  }
}
